#include<stdio.h>
#define Maxsize 50
typedef struct {
	int* elem;
	int length;
}Sq;
void Init(Sq& L) {
	L.elem = new int[Maxsize];
	L.length = 0;
}
void Insert(Sq& L,int num) {
	if (L.length >= Maxsize) {
		return;
	}
	L.elem[L.length] = num;
	L.length++;
}
void Add(Sq& L, int data, int item) {
	int pos = L.length;
	for (int i = 0; i < L.length; i++) {
		if (L.elem[i] == item) {
			pos = i;
			break;
		}
	}
	for (int i = L.length - 1; i >= pos; i--) {
		L.elem[i + 1] = L.elem[i];
	}
	L.elem[pos] = data;
	L.length++;
}
void display(Sq& L) {
	for (int i = 0; i < L.length; i++) {
		printf("%d ", L.elem[i]);
	}
}
int main() {
	Sq L;
	int n, item, data,num;
	Init(L);
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf("%d", &num);
		Insert(L, num);
	}
	scanf("%d", &item);
	scanf("%d", &data);
	Add(L, data, item);
	display(L);
	return 0;
}
